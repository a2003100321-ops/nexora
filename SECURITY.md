# 安全基线

## 不得提交的内容

- Android Keystore、证书私钥、签名密码和恢复码；
- `local.properties`、GitHub Token、Cookie、登录 Token、服务账号文件；
- 用户日志、真实用户配置、访问历史或个人媒体库清单；
- 来源不明的 AAR、SO、JAR、APK 或其他二进制；
- PickTV 源码压缩包、解压目录或未完成来源台账的派生代码。

如敏感信息误入 Git 历史，应立即停止发布并轮换相应凭据；仅删除工作区文件并不能清除 Git 历史。

## 旧配置与插件

旧配置和插件均按不可信输入处理。M0～M2 只允许合成语料的静态读取与测试，不加载、不反射实例化、不执行 JAR、JavaScript、Python 或原生库，不关闭 TLS 校验，也不允许主进程任意动态代码执行。

## 漏洞报告

公开仓库启用后，请通过仓库的私密安全报告渠道提交漏洞，不要在公开 Issue 中粘贴 Token、Cookie、真实配置或用户日志。
